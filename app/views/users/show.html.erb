<%= provide(:title, "Show") %>
<div class="row">
	<aside class="col-md-4" id="aside">
		<section class="user-info">
			<h1>
		    <%= gravatar_for(@user, size: 40) %>
		    <%= @user.name %>
      </h1>
     </section>
  </aside>
</div>
